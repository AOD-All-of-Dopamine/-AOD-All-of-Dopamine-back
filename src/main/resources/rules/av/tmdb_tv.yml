# src/main/resources/rules/av/tmdb_tv.yml

platformName: TMDB_TV
domain: AV
schemaVersion: 3

fieldMappings:
  # === Master & Platform Mappings ===
  name: master_title
  original_name: original_title
  poster_image_url: poster_image_url
  overview: synopsis

  # === Domain Mappings (for domainDoc) ===
  av_type: domain.av_type
  id: domain.tmdb_id
  first_air_date: domain.release_date
  genres: domain.genres

  # === Platform Attributes ===
  watch_providers: platform.attributes.watch_providers
  episode_run_time: platform.attributes.runtime
  number_of_seasons: platform.attributes.season_count
  cast: platform.attributes.cast
  crew: platform.attributes.crew

domainObjectMappings:
  av_type:
    targetField: avType
    type: string
  tmdb_id:
    targetField: tmdbId
    type: integer
  release_date:
    targetField: releaseDate
    type: date
  genres:
    targetField: genres
    type: list

normalizers:
  - type: collapse_spaces
    fields: [master_title, original_title]
  - type: nfkc
    fields: [master_title, original_title]
