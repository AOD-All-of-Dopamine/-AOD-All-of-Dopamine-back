-- ============================================================================
-- V9: rec_chain.skipped_keys (2026-09)
-- 근거: 4번 하위 프로젝트 설계 §4(커버리지 대응)·§7.
--
-- 코퍼스는 백엔드 카탈로그보다 훨씬 크다. 라우터가 준 후보 중 DB 에 없거나 성인이라
-- 버린 것은 seen(=서빙한 content_id)에 들어가지 않아, 다음 쪽에서 같은 후보가 다시 와
-- 버퍼를 통째로 먹는다. 버린 후보의 "키"를 체인에 기억해 다음 요청의 excluded 로 보낸다.
--
-- 값 형식: "platform:key" (예: steam:730 · tmdb:movie_603 · webnovel:12345).
-- 상한 2,000 개는 애플리케이션(ChainService.SKIPPED_MAX)이 지킨다 — DB 제약으로 걸면
-- 초과 시 추천 응답이 실패하는데, 이 값은 있으면 좋은 최적화지 정확성 조건이 아니다.
--
-- V8 은 고치지 않는다(로컬·운영에 이미 적용됨 — 체크섬).
-- ============================================================================

ALTER TABLE aod_rec.rec_chain
    ADD COLUMN IF NOT EXISTS skipped_keys text[] NOT NULL DEFAULT '{}';
